# Environment Configuration — copy to .env and update values
# DO NOT commit .env to version control

# PostgreSQL
POSTGRES_PASSWORD=fraudengine_local

# AES-256-GCM field encryption key (must be at least 32 characters)
APP_ENCRYPTION_KEY=your-aes-256-key-minimum-32-chars!!
